<%= form_for(@propreview) do |f| %>
  <% if @propreview.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@propreview.errors.count, "error") %> prohibited this propreview from being saved:</h2>

      <ul>
      <% @propreview.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <%= f.hidden_field :user_id, value: @user %>
 <%= f.hidden_field :properity_id, value: @properity %>
  <div class="field">
    <%= f.label :rating %><br>
    <%= f.radio_button :rating, "1" %>1
    <%= f.radio_button :rating, "2" %>2
    <%= f.radio_button :rating, "3" %>3
    <%= f.radio_button :rating, "4" %>4
    <%= f.radio_button :rating, "5" %>5
  </div>
  <div class="field">
    <%= f.label :Review %><br>
    <%= f.text_area :rev_desc %>
  </div>
  <div class="actions">
    <%= f.submit  %>
  </div>
<% end %>
